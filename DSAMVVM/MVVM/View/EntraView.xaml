<UserControl x:Class="DSAMVVM.MVVM.View.EntraView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DSAMVVM.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid>
        <Button Content="{Binding AuthButtonText}" 
                Command="{Binding AuthToggleCommand}" 
                HorizontalAlignment="Left"
                VerticalAlignment="Bottom"
                Margin="20"
                Style="{DynamicResource Button.Rounded}" />

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,50,0,0">
            <TextBlock Text="{Binding EntraResults}" 
                       Foreground="#CCD3DA" 
                       FontSize="14" 
                       TextWrapping="Wrap" 
                       TextAlignment="Center" 
                       MaxWidth="500" />
        </StackPanel>
    </Grid>
</UserControl>