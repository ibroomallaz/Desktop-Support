<Window x:Class="DSAMVVM.MVVM.View.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="True"
        Width="560" Height="320"
        ShowActivated="False"
        Focusable="False">
    <Border CornerRadius="16" Background="#1A1D23" BorderThickness="1" BorderBrush="#2A2F37" Padding="24" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- centered logo/title -->
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,12,0,24">
                <TextBlock Text="Desktop Support App" Foreground="#E6E6E6" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock Text="University of Arizona" Foreground="#A0A6B2" FontSize="14" Margin="0,6,0,0" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- bottom status row centered as a unit -->
            <Grid Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ProgressBar Grid.Column="0" IsIndeterminate="True" Height="6" Width="200"/>
                <TextBlock Grid.Column="2" x:Name="StatusText" Text="Starting…" Foreground="#A0A6B2" FontSize="12" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>
